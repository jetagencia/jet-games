# JETBALL

Reimaginación 2026 de **JezzBall** (Microsoft, 1992). Mobile-first PWA empaquetada como APK Android via Capacitor.

**Estado**: el proyecto fuente vive aún en `C:\Users\torra\Desktop\jettball-pwa\` (separado del monorepo). Cuando tenga sentido (probablemente al migrar a Capacitor + Play Store closed testing), se mueve acá adentro.

**Razón de la separación temporal**: el flujo bubblewrap + keystore ya está montado en jettball-pwa. Migrarlo ahora a este repo agrega complejidad sin valor inmediato.

**Plan de migración**: cuando se inicie v1.1 con Capacitor wrap + AdMob + Play Billing → mover todo el código a `jet-games/jetball/`.

Ver el progreso de JETBALL en [`/jettball-pwa/`](../../jettball-pwa) (carpeta separada en el Desktop, fuera de este repo).
